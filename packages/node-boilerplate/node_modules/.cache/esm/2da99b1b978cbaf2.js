let arg,chalk,validateOptions;_978‍.x([["parseArgVector",()=>parseArgVector]]);_978‍.w("arg",[["default",["arg"],function(v){arg=v}]]);_978‍.w("chalk",[["default",["chalk"],function(v){chalk=v}]]);_978‍.w("./validationHelpers",[["validateOptions",["validateOptions"],function(v){validateOptions=v}]]);



       const parseArgVector = argv => {
  try {
    const parsedArgs = arg(
      {
        // "--database": String,
        // "--auto-template-watcher": Boolean,
        "--help": Boolean,
        // "-d": "--database",
        // "-w": "--auto-template-watcher",
        "-h": "--help"
      },
      { argv }
    );
    return validateOptions(parsedArgs);
  } catch (err) {
    if (err.code === "ARG_UNKNOWN_OPTION") {
      log();
      log("Seems like you entered some invalid option(s):");
      log(chalk.redBright(err.message));
      log();
      log(
        `Use ${chalk.blueBright("create-node-app")} ${chalk.greenBright(
          "--help"
        )} for all valid options.`
      );
    } else {
      throw err;
    }
  }
};
